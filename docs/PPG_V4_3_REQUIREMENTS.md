# Requirements

Python 3.10+ recommended.

Core packages:
- numpy
- pandas
- scipy
- scikit-learn
- joblib

The package reuses the validated V4.2 feature extractor and preparation
workflow; no new external service is required.
