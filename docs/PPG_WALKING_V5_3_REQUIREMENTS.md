# V5.3 Requirements

Python packages: numpy, pandas, scipy, scikit-learn, joblib, wfdb.

Run all commands from repository root. Every tool inserts repo root into sys.path so `python tools\...py` works directly.

Required input:
datasets/raw/WristPPGExercise/RECORDS
datasets/raw/WristPPGExercise/*.hea
datasets/raw/WristPPGExercise/*.dat
datasets/raw/WristPPGExercise/*.atr
