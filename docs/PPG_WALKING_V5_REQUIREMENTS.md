# V5 Requirements

Install in the SafeBand virtual environment:

```powershell
pip install wfdb numpy scipy pandas scikit-learn joblib
```

The V5 preparation script expects the raw PhysioNet directory:

```text
datasets/raw/WristPPGExercise/
```

Do not modify raw files.
